services:
  nginx:
    build:  /nginx/Dockerfile
    ports:  "443:443"
    restart:  on_failure

  mariadb:
    build:  /nginx/Dockerfile
    volumes: mariadb_data:/home/aweaver/data/mariadb
    restart: on_failure
    depends_on: nginx

  wordpress:
    build:  /nginx/Dockerfile
    volumes:  wordpress_data:/home/aweaver/data/wordpress
    restart:  on_failure
    depends_on:
      - nginx
      - mariadb
